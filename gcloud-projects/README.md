#gcloud

##commands

* list config
```$xslt
gcloud config list
```

* set a project to work on
```$xslt
gcloud config set project [YOUR_PROJECT_ID]
```

* deploy to appengine

```$xslt
gcloud app deploy
```